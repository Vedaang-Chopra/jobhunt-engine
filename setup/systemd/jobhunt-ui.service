[Unit]
Description=Job-hunt NiceGUI dashboard (ui.app)
PartOf=graphical-session.target
After=graphical-session.target jobhunt-automation-chrome.service

[Service]
Type=simple
WorkingDirectory=%h/git/jobhunt-engine
Environment=JOBHUNT_HOME=%h/git/jobhunt-engine/jobhunt-data
Environment=PATH=/usr/local/bin:/usr/bin:/bin:%h/git/jobhunt-engine/.venv/bin
ExecStart=%h/git/jobhunt-engine/.venv/bin/python -m ui.app
Restart=always
RestartSec=10

[Install]
WantedBy=graphical-session.target
